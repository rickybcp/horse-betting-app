# Cloud Storage Configuration
# Set these environment variables to enable cloud storage
GCS_BUCKET_NAME=your-horse-betting-bucket
GOOGLE_CLOUD_PROJECT=your-project-id

# Optional: Path to Google Cloud service account key file
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# App Configuration
FLASK_ENV=production
FLASK_DEBUG=false

# Current Race Day (can be overridden via API)
CURRENT_RACE_DAY=2025-08-09
